server:
  address: 0.0.0.0
  port: ${SERVER_PORT:8082}

spring:
  application:
    name: spark-employment-platform
  datasource:
    url: ${MYSQL_JDBC_URL:jdbc:mysql://localhost:3306/spark_employment?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai}
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.nologging.NoLoggingImpl

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

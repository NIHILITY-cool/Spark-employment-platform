# 进度日志

## 2026-07-14
- 推荐继续收紧：学历改为候选硬过滤，技能退出评分，权重调整为经历 40、方向 30、城市 10、最低期望薪资 10、行业 5、时效 5；明确方向时过滤方向证据不足的岗位。就业期望只保留最低月薪，技能清单增加分页，推荐侧栏改为轻量画像证据概况。
- 学生画像新增项目、实习、获奖三类结构化经历，支持新增、编辑、删除；推荐从六维调整为八维 100 分，经历相关性提高到 30 分、技能降为 15 分辅助证据，新增行业和岗位时效维度，岗位方向不再硬过滤候选。推荐解释展示经历领域词命中，岗位详情移除覆盖率较低的技能抽取区块。

## 2026-07-13
- 高校端新增行业薪资分布：后端按岗位名称与岗位描述关键词将岗位固定归入金融、商贸与消费、医药生物、科技、制造业、农业与食品、服务业、建筑与房地产、教育、其他十类，并按地区聚合岗位量、有效薪资样本、平均月薪及 5 档月薪区间；前端使用 ECharts 堆叠柱与平均月薪折线联动展示，支持提示详情、图例筛选、移动端缩放与地区同步更新。后端 Maven 测试、高校端 Playwright 桌面/移动端测试及前端生产构建通过。
- 修复 NCSS 岗位描述重复：Spark 清洗仅保留原始清洗后的 `job_description`，不再拼接格式不稳定且可能为描述副本的职责、要求字段；已恢复虚拟机 HDFS 与 Spark 服务并按同日期快照完成清洗、市场统计、技能提取和 MySQL 全量导出。当前 MySQL 为 12,000 个岗位、2,505 条技能记录和 588 条统计记录，接口抽样确认重复描述已消除。
- 学生端岗位卡片改为可点击的岗位详情抽屉：展示基础信息、岗位描述、Spark 提取技能和原岗位外链，加入加载骨架、异常提示、Esc 关闭与低强度过渡动效；移除无效的“查看推荐”按钮。学生端与高校端统一使用右上角“返回初始页”入口和图标样式。前端 Playwright 11 项测试及生产构建通过。
- 恢复临时共享访问：本机 Vite 与 ngrok 进程重启；因本机前端进程不能直接访问虚拟机网段，Vite `/api` 改经本机 `18082` SSH 隧道转发至虚拟机 Spring Boot `8082`，避免向公网暴露虚拟机端口。
- 项目级安装 `taste-skill`（安装名 `design-taste-frontend`），用于后续前端视觉设计与页面重构。
- 启动联调服务：本机 Vue/Vite 前端运行于 `http://127.0.0.1:5173/`；虚拟机 Spring Boot API `http://192.168.64.2:8082/api/health` 健康，当前连接 12,000 条岗位数据。
- 为临时共享访问配置 Vite 同源 `/api` 代理到虚拟机后端，已验证代理健康检查；ngrok 隧道待本机配置账号认证令牌后启动，未公开 MySQL、HDFS 或 Spark 端口。
- ngrok 临时共享隧道已启动并验证：公开页面与同源 `/api/health` 均返回 200。Vite 白名单仅允许 `*.ngrok-free.dev`；公开入口只转发本机前端及其到虚拟机 API 的代理，MySQL、HDFS、Spark 管理端口仍未暴露。
- 重启 ngrok 临时隧道；当前 ngrok CLI 已弃用手动 `--region ap`，自动选择最低延迟节点，公开地址保持可用。
- 虚拟机恢复在线后重新启动 Spring Boot 后端；`8082/api/health` 及公开 ngrok 代理接口均返回正常，后端已连接 MySQL 的 12,000 条岗位数据。
- 公开入口曾因 ngrok 临时进程结束而失效，现已恢复并放入本机 `employment-ngrok` tmux 会话持续运行；公开 API 健康检查返回正常。

## 2026-07-12
- 学生端显示密度优化：岗位市场改为后端真实分页（每页 12 条），技能信号改为每页 8 条的紧凑分页；筛选会回到首页，分页控件支持首尾省略与移动端收缩。新增页面契约测试覆盖岗位第二页请求和技能下一组展示；Playwright 10 项通过，前端生产构建通过。
- 新增智联招聘 2026-07-11 原始快照（578 条）接入规范：Spark 以岗位链接生成稳定 ID，仅解析月薪范围；清洗、技能提取、市场统计和 MySQL 导出将按同一日期快照重算。
- 修正城市口径：直辖市保留为城市；省级地点仅在区域或岗位详情存在明确城市证据时提升为城市，不能验证的省级地点不再进入城市筛选、城市统计和高校区域矩阵。
- 前端将岗位筛选、学生画像/就业期望与高校分析的原生下拉统一为可复用的可搜索选择组件，候选面板固定高度、支持滚动及键盘选择。
- 学生端将岗位市场与技能信号拆为独立分页；技能页新增技能需求强度排名、聚焦技能详情和城市岗位热度。

## 2026-07-11
- 前端新增身份选择初始页，将学生端（岗位市场、画像、推荐）与高校端（培养需求分析）分离为独立工作区；登录暂未接入，当前选择身份后直接进入演示工作台以方便测试。
- 同步 plan.md 更新：后端从 Flask 切换为 Spring Boot
- 新增 infrastructure 的 spark/mysql/redis 子目录
- 新增 Redis 缓存设计文档
- 更新前后端目录结构
- 合并国聘、猎聘、ncss 三站采集代码、原始数据、清洗结果与质量报告；移除代码中的会话凭据。
- 在虚拟机启动 HDFS、YARN 与 Spark Standalone；HDFS 已创建 Raw、Cleaned、Warehouse、Output、Archive 分层目录。
- 三站原始 CSV 已上传至 HDFS Raw：国聘 531 KB、猎聘 1.7 MB、ncss 20.9 MB。
- 新增并验证 PySpark 清洗与市场统计任务：Cleaned/DWD 统一岗位表共 11,559 条（国聘 297、猎聘 752、ncss 10,510），市场统计已写入 HDFS Output。
- 新增岗位类别、技能与别名字典，并在 Spark 上完成岗位分类与技能提取；岗位画像写入 `warehouse/dwd/job_profiles/date=2026-07-11`，岗位技能明细写入 `cleaned/job_skills/date=2026-07-11`，热门技能与类别统计写入 HDFS Output。
- 虚拟机已安装并启动 MySQL 8.0；创建 `spark_employment` 数据库、`job`、`job_skill`、`market_statistic` 表及仅本机可读的应用连接配置。
- 新增并验证 Spark JDBC 导出：MySQL 当前包含岗位 11,559 条、岗位技能 2,647 条、市场统计 431 条；JDBC 驱动由 Spark 提交时加载，连接配置未提交仓库。
- 本机已初始化并启动 Vue 3 + Vite 前端（`http://localhost:5173/`）；岗位市场页支持真实 API 读取与后端未启动时的明确演示数据回退，生产构建已通过。
- 完善 Spring Boot 后端：实现统一异常响应、健康检查、岗位筛选/详情/筛选项、市场统计总览、学生画像/技能/就业期望，以及基于岗位技能的实时 Top10 推荐和技能差距接口；学生演示链路已完成端到端验证。
- 后端已在虚拟机 Java 17 下打包并启动，监听 `http://192.168.64.2:8082`（8080 由 Spark Master UI 占用）；本机前端已切换到 8082，跨域与真实岗位/市场数据访问均已验证。
- 修复本机前端岗位筛选：城市与岗位方向不再从当前页或写死列表读取，改为调用 `/api/jobs/filters` 获取所有有效岗位的筛选项；前端构建已通过。
- 将城市与岗位方向改为标准可搜索选择框：左侧输入实时过滤匹配项，右侧箭头独立控制展开/收起；候选面板固定为约 5 条可见项高度，超出内容在面板内滚动，支持鼠标与上下键/回车选择；前端构建已通过。
- MySQL 已新增 `student`、`student_skill`、`job_preference` 三张业务表；保留一条 `demo001` 演示学生画像及 Python、SQL、Spark 技能，用于接口演示。
- Spark 提交须使用 `spark://192.168.64.2:7077`；`hwadee01` 当前解析到回环地址。
- 统一城市字段展示口径：ETL、后端与前端均移除城市名称末尾的“市”，无法细分的“中国”统一为“全国”，并同步修正 MySQL 岗位及城市统计存量数据。
- 修正 Docker Compose、Nginx 与 Spring Boot 之间的端口和 MySQL 连接变量不一致问题。
- 根据真实数据可得性调整项目范围：公开岗位作为真实主数据，学生采用自主填写，高校端采用授权匿名导入或明确演示队列；不建设就业跟踪，近期岗位数据只做需求信号与场景推演。
- 推荐评分对齐六维权重，增加技能证据置信度、学历与经历分，避免稀疏技能岗位和高经验岗位获得不合理高分；新增评分单元测试。
- 完成学生端前端闭环：画像编辑、技能增删与等级、就业期望、保存后重算、Top10 六维匹配轨道、技能差距、岗位展开、真实外链和本地收藏均可交互。
- 新增 Playwright 桌面与移动端测试，覆盖真实 API 画像加载、推荐展开、收藏状态和横向溢出检查；前端生产构建通过。
- 新增高校培养方向场景分析 API：基于 Spark 清洗并写入 MySQL 的真实岗位，按专业/地区聚合岗位样本、低经验门槛比例、薪资、行业、学历、技能和地区岗位方向矩阵。
- 新增高校培养参考页：支持专业与地区筛选、技能组合点击、场景覆盖率即时更新、行业/学历结构、需求矩阵和规则建议；不使用毕业生去向数据，不输出培养质量结论。
- 高校页 Playwright 契约测试桌面/移动端 2 项通过，真实虚拟机 API 集成测试 1 项通过；后端 Maven 测试与前端生产构建通过。
- 复核当前项目状态：虚拟机 API 健康，2026-07-11 批次已包含国聘 297、猎聘 752、NCSS 10,408、智联 543，共 12,000 条岗位；城市筛选输出为无“市”后缀的规范名称。虚拟机后端 Maven 单元测试 5/5 通过，本机前端生产构建通过；本机 Python 环境尚未安装 `pytest`，数据处理测试待依赖补齐后复跑。

## 2026-07-10
- 初始化项目目录结构
